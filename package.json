{
  "name": "jericho-companion-robot",
  "version": "1.0.0",
  "private": true,
  "description": "Robot-user test system for Jericho Companion",
  "scripts": {
    "build:testpage": "node scripts/build-test-page.js",
    "emulator": "firebase emulators:start --project jericho-test --only firestore,auth",
    "robot": "node scripts/run-robot.js",
    "report": "node scripts/report.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "esbuild": "^0.28.2",
    "firebase": "^10.13.0",
    "firebase-tools": "^13.29.1"
  }
}
